# Karan Systems — Secure Agentic RAG
# Stack: create_agent + Google Drive ingestion + Pinecone + Gemini

# ── Core LangChain (create_agent backed by LangGraph) ───────
langchain>=0.3.25
langchain-core>=0.3.55
langchain-community>=0.3.20
langchain-text-splitters>=0.3.8

# ── LangGraph (powers create_agent) ─────────────────────────
langgraph>=0.2.70
langgraph-checkpoint>=2.0.10

# ── LLM + Embeddings (Google Gemini) ────────────────────────
langchain-google-genai>=2.0.11
google-genai>=0.3.0

# ── Vector Store (Pinecone) ──────────────────────────────────
langchain-pinecone>=0.2.4
pinecone>=5.4.2

# ── Google Drive Ingestion ───────────────────────────────────
gdown>=5.2.0
google-api-python-client>=2.145.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.1

# ── Document Parsers ─────────────────────────────────────────
pypdf>=4.3.0
python-pptx>=1.0.2
openpyxl>=3.1.5
python-docx>=1.1.2
unstructured>=0.16.10
markdown>=3.7
networkx>=3.0

# ── UI ───────────────────────────────────────────────────────
streamlit>=1.40.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.1
rich>=13.9.4
pandas>=2.2.3
tabulate>=0.9.0
tqdm>=4.67.1

# ── Model Context Protocol (MCP) ─────────────────────────────
mcp>=1.1.2
langchain-mcp-adapters>=0.1.0
pydantic>=2.0.0
